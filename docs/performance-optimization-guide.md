# sanye_anime 全功能性能优化方案与教学

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.1 |
| 文档状态 | 基线（本地代码、测试与构建已验证；生产容量待环境） |
| 关联文档 | [性能测试报告](./performance-test-report.md)、[全系统监控方案](./monitoring-design.md)、[技术架构](./technical-architecture.md)、[质量门禁](./quality-gates.md) |
| 更新时间 | 2026-09-10 |

本文档覆盖客户端、管理平台、服务端、桌宠和性能门禁。这里的“所有功能”是指对每个功能域完成性能路径审计，并采用一组共享的基础优化；不是在缺少基线时强行改写每一个函数。只有具备命令、产物或测试证据的项目才标记为已验证，真实流量、外部 AI 和独立生产环境结果保留为待环境。

## 当前核对（2026-09-10）

本次前端类型和构建通过，存在大分块警告；没有重新压测或执行 `pnpm perf:check`，不更新历史性能数字为当前达标结论。复跑采用当前工具链，证据边界见[当前审计](./current-status-audit.md)。

更新记录：2026-09-10，v1.1，按当前代码与进度校正本文事实或证据范围；依据上述源码、任务与审计引用。

## 1. 目标与验收口径

性能优化必须同时守住四条边界：功能行为不变、错误与权限状态不退化、资源消耗有上限、收益可以复测。

| 层级 | 主要指标 | 本地门禁 | 生产建议 |
| --- | --- | --- | --- |
| 浏览器首屏 | 入口 JavaScript/CSS、LCP、CLS | `pnpm perf:check` | LCP ≤ 2.5 秒、CLS ≤ 0.1 |
| 页面交互 | 路由切换、长列表渲染、重复请求 | 类型检查、构建、页面回归 | INP ≤ 200 毫秒 |
| 服务接口 | p95、错误率、数据库查询数 | 单元测试、模块测试 | 核心读接口 p95 ≤ 200 毫秒、错误率 0% |
| 后台任务与导入 | 并发上限、超时、队列长度 | 受控线程池与超时测试 | 按来源限流，禁止无界并发 |
| 桌宠 | 隐藏窗口 CPU、入口资源 | 构建与可见性逻辑 | 后台不可见时不持续推进帧循环 |

前端告警阈值以[全系统监控方案](./monitoring-design.md)为准；历史接口 QPS 与压缩数据见[性能测试报告](./performance-test-report.md)。

## 2. 全功能覆盖矩阵

| 功能域 | 性能风险 | 当前方案 | 状态与证据 |
| --- | --- | --- | --- |
| 客户端首页、片库、搜索、排期、我的、官网 | 首屏下载、离屏卡片绘制、重复请求 | 路由懒加载与意图预取、请求取消、图片懒加载、离屏渲染跳过；首屏横幅提高获取优先级 | 已验证：客户端类型检查、构建；入口 241.5 KiB 原始/67.0 KiB gzip |
| 播放器与 Anime4K | HLS、ArtPlayer、Anime4K 体积大且运行成本高 | 仅进入详情或启用增强时动态加载，播放器销毁时释放资源 | 已有实现；独立媒体块不计入首屏入口预算 |
| AI 会话与模型配置 | SSE 长连接、模型延迟、并发生成 | 流式响应、生成线程池、额度与队列指标；真实供应商仍受账户环境约束 | 本轮未改行为；真实模型容量为待环境 |
| 管理端全部页面 | 全量 Element Plus 进入登录首屏、权限请求串行 | 按需组件/样式注册、页面路由懒加载与意图预取、用户信息和路由并行单飞 | 已验证：入口约 1.46 MB→0.71 MB，约减少 51%；gzip 约 405 kB→209 kB，约减少 48% |
| 动漫目录与详情 | 空关键词仍读取简介表、详情重复校验查询 | 空关键词跳过简介映射；详情查询直接限定已发布状态，去掉额外 `cardOf/statusOf` 往返 | 已验证：anime 模块 80/80 |
| 首页聚合 | 缓存击穿、公共线程池争用、线程上下文丢失 | Redis TTL、空值缓存、随机抖动、单飞；首个请求在线程内回源，等待者复用 Future | 已验证：同线程回源和 8 路并发单飞测试 |
| 搜索与导入 | 外部慢请求、重复抓取、无界并发 | 外部缓存和请求合并、8 路受控抓取、批量写入、同页复用 | 已有实现；外部来源耗时为待环境 |
| 反馈处理 | 状态更新后全表读取再内存查找 | 更新后按主键只读取目标反馈 | 已验证：feedback 模块 7/7 |
| 收藏、文件、认证、网关、任务、管理后端 | 连接池、压缩、缓存、登录哈希成本 | HikariCP 容量、HTTP 压缩、静态资源缓存、仪表盘 TTL 缓存、鉴权单飞 | 已有实现；历史实测见性能测试报告 |
| 桌宠 | 窗口隐藏后定时器仍持续推进动画 | 监听 `visibilitychange`，隐藏时清除定时器，恢复时续播 | 已验证：桌宠类型检查与构建 |
| 持续门禁 | 后续提交重新放大全量首屏资源 | `perf:check` 构建三端并检查入口 gzip 预算 | 已验证：client 67.0/75、admin 204.5/220、pet 32.0/70 KiB |

## 3. 本轮实现说明

### 3.1 管理端按需加载

优化前，`main.ts` 全量安装 Element Plus，并由 Vite 固定生成约 919 kB 的框架块、104 kB 的图标块和389 kB 的全量样式。即使用户只打开登录页，浏览器也必须下载、解析和编译这部分代码。

本轮只注册实际使用的组件与对应样式，删除强制把整个包放进固定分块的配置。业务页面继续按路由拆分；用户把鼠标或键盘焦点移向导航项时，目标页面代码会提前获取。首次权限恢复把 `getInfo` 和 `getRouters` 并行执行，并使用单飞 Promise 合并重复导航触发。

收益来自三部分：减少网络字节、减少 JavaScript 解析/编译、把页面加载从“点击后开始”前移到“表达导航意图时开始”。

### 3.2 客户端渲染路径

客户端原有路由、HLS、ArtPlayer 和 Anime4K 已经动态加载，本轮保留这一边界。首页横幅增加高获取优先级和异步解码；卡片、搜索结果和相似作品使用 `content-visibility: auto`，浏览器可以跳过视口外元素的样式、布局和绘制，并通过固有占位尺寸保持滚动稳定。

该规则适合高度可估计的长列表，不应直接用于首屏标题、播放器或动态高度完全未知的区域，否则可能造成跳动。

### 3.3 服务端并发与查询

首页缓存未命中时需要“一个请求回源，其余请求等待同一结果”。旧实现把回源提交到 JVM 公共 `ForkJoinPool` 后，请求线程再同步等待，这会和其他公共池任务竞争，也会丢失请求线程上下文。本轮改为首个请求在线程内执行回源，其他请求只等待同一个 `CompletableFuture`。单飞语义不变，但资源边界更清晰。

目录列表仅在关键词搜索时才需要简介字段，因此空关键词列表不再读取简介映射。详情存储查询直接带 `status = '已发布'`，业务层不再先查卡片、再查状态、最后查详情。反馈状态更新后直接按主键回读，不再加载全部反馈后执行线性扫描。

### 3.4 桌宠后台节流

动画帧由递归定时器推进。窗口隐藏时继续运行不会产生可见价值，却会唤醒渲染进程。本轮在 `document.hidden` 时停止定时器，重新可见时继续，从而降低后台唤醒次数，同时不改变前台动画状态机。

## 4. 性能优化教学

### 4.1 先建立基线，再选择手段

1. 明确用户动作，例如首次登录、打开详情、搜索、导入或播放。
2. 分解关键路径：下载 → 解析 → 渲染 → API → 数据库/外部服务。
3. 记录 p50、p95、错误率、请求数、查询数、入口字节和 CPU，而不是只看平均值。
4. 找到占比最大的步骤，只改变一个主要变量并复测。
5. 把收益变成门禁或监控，否则优化会随依赖和功能增长而消失。

### 4.2 估算模型

页面等待时间可以近似看成：

```text
页面时间 ≈ 网络往返 + 下载时间 + JS 解析/执行 + 渲染 + 最慢关键接口
```

串行接口的总时间接近各步骤相加，并行接口的时间接近最慢一步。并行不是越多越好：外部抓取并发必须小于来源、线程池和连接池能稳定承受的最小容量。

数据库查询应关注数量和扫描规模。按主键查询通常接近 `O(log n)` 或索引常数级访问；“读取全部再筛选”需要传输和构造 `n` 条记录，是 `O(n)`，数据越大差距越明显。

### 4.3 缓存与单飞的区别

缓存解决“重复计算”，单飞解决“同一时刻大量缓存未命中”。只有缓存没有单飞，缓存刚过期时仍可能出现几十次并发回源；只有单飞没有缓存，则每一批请求仍要回源。TTL 抖动用于避免大量 key 同时失效，空值短 TTL 用于阻止不存在数据反复穿透。

### 4.4 体积预算怎么定

预算应基于当前可接受基线并保留有限增长空间。本项目门禁统计 `index.html` 直接引用的 JavaScript 和 CSS gzip 总量，不把按功能动态加载的播放器、Anime4K 和桌宠精灵图错误计入入口包。媒体资源仍应通过图片格式、缓存头和授权来源单独治理。

当前预算：客户端 75 KiB、管理端 220 KiB、桌宠 70 KiB。若确有业务需要提高预算，必须在评审中附前后产物和用户收益，不能只修改阈值让 CI 通过。

## 5. 验证与复跑

```powershell
pnpm typecheck
pnpm perf:check
. ./sanye_deploy/use-local-toolchain.ps1
mvn -B -f sanye_server/pom.xml -pl sanye-server-anime,sanye-server-feedback -am test
pnpm docs:check
```

本轮本地证据：

| 命令 | 结果 |
| --- | --- |
| 三端 `typecheck` | 客户端、管理端、桌宠全部通过 |
| `pnpm perf:check` | 三端构建通过，入口 gzip 预算 3/3 通过 |
| Maven 定向 Reactor | core 18、web 10、anime 80、feedback 7，共 115 项通过 |
| `pnpm docs:check` | 586/586，通过 |

真实生产 QPS、外部 AI SSE、外部动漫来源响应时间和独立压测机 CPU/GC 仍为待环境，不使用本地构建结果代替容量结论。

## 6. 回滚与排障

| 现象 | 检查 | 回滚方向 |
| --- | --- | --- |
| 管理端组件无样式或不可用 | 检查组件注册和对应 `style/css` 导入 | 补齐单个组件；紧急情况下恢复全量注册 |
| 导航预取增加无效流量 | 核对触发范围和网络面板 | 移除对应悬停/聚焦预取，不影响路由懒加载 |
| 长列表滚动占位异常 | 检查 `contain-intrinsic-size` 与实际卡片高度 | 调整占位，或只移除该选择器的离屏规则 |
| 首页缓存请求等待异常 | 核对 in-flight Future 是否完成、异常是否传播 | 回退为同步回源并保留缓存，不恢复公共池阻塞模型 |
| 草稿详情意外公开 | 检查详情 SQL 的发布状态条件 | 阻断接口并恢复 `status = '已发布'` 条件 |

## 更新记录

| 日期 | 版本 | 变更 | 依据 |
| --- | --- | --- | --- |
| 2026-08-26 | v1.0 | 建立全功能覆盖矩阵、前后端与桌宠优化说明、性能教学、入口体积门禁和复跑证据 | `pnpm perf:check`、Maven 定向 Reactor、三端类型检查 |
