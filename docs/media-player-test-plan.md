# 公开媒体导入与播放器测试计划

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.18 |
| 文档状态 | 基线 |
| 关联文档 | [公开媒体导入评审](./media-import-review.md)、[媒体开发设计](./media-player-development.md)、[测试策略](./testing-strategy.md)、[质量门禁](./quality-gates.md) |
| 唯一基准 | 否；本功能测试基准 |
| 更新时间 | 2026-09-21 |

## 刷新对齐与长帧治理验收（2026-09-20）

更新记录：2026-09-20，v1.8，补充 VQ-19～VQ-21 的验收口径；本轮证据为受控无头浏览器与单元测试，见[播放器测试报告](./media-player-test-report.md)。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-19 | 目标不高于刷新能力时按刷新边界逐帧调度，非整数倍刷新不漏画；刷新回调不交付时回退计时器时钟 | 单元测试覆盖非整数倍比例、刷新交付确认与饥饿回退、计时器跳过过期时隙；无头浏览器切换刷新时钟后每个窗口绘制时隙等于刷新回调次数且计时器唤醒为零 |
| VQ-20 | 取消逐帧同步完成回读；按长帧比例与绘制耗时判定抖动和压力 | 单元测试覆盖长帧比例替代 P95 间隔、缺少长帧比例时保留旧口径、持续绘制耗时触发渲染压力；帧间隔最大值与绘制耗时窗口存档 |
| VQ-21 | 展示调度不得改变输出尺寸、清晰度档位、增强回退门限与菜单 | 浏览器窗口输出尺寸与生效档位与请求一致；物理高刷、整片、跨显卡、功耗与桌面安装物保持待环境 |

进度条与定位补充验收（VQ-22、VQ-23）：

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-22 | 进度条跟随播放位置连续更新，不出现固定滞后与静止段 | 四类配置下写入次数与页面渲染帧数一致、最长静止为 0、目标位置滞后 P95 为 0；暂停、拖动预览与销毁不产生额外写入 |
| VQ-23 | 拖动定位只提交一次、不重复请求分片，并记录补帧恢复与原画窗口 | 缓存命中拖动在百毫秒内完成定位；补帧层恢复时长、原画揭示窗口、主线程长任务与呈现节奏均写入报告；仅增强路径的主线程执行已由 VQ-28 移入渲染 Worker，定位后原画窗口保留为未解决项 |

画质稳定性补充验收（VQ-24）：

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-24 | 展示层与档位在稳定播放和短时抖动下保持稳定，只有内容持续停止推进才回退 | 稳定窗口无整层切换、无增强/帧率档位变化、无缓冲事件；注入 0.4 秒主线程卡顿后补间层仍在显示；注入 1.5 秒中断后回退并能自动恢复；落后窗口通过 `stallFrames` 可观测 |

起播画质与增强稳定性验收（P6，VQ-25～VQ-31 已实施；VQ-31 的 120/144/165Hz 物理呈现待环境）：

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-25 | 搜索点击到高档到位的起播时间线 | 使用 `e2e/startup-quality-timeline.test.cjs`；有历史且带宽足够时高档到位 ≤ 0.5 秒，冷启动与带宽不足仍出首帧 |
| VQ-26 | 增强档位降档与恢复 | 使用 `e2e/enhancement-recovery.test.cjs`；瞬时抖动不永久降档；恢复需冷却与稳定样本门槛，过程中不出现档位往返；回退记录与菜单跟随实际档位 |
| VQ-27 | 源帧率达标时的补帧开销 | 使用 `e2e/interpolation-bypass.test.cjs`；真实 60 FPS 播放窗口跳过光流分析且不产出合成帧、画布继续呈现；同源 `always` 对照显示运动场上传成本显著更高；源帧率不足时恢复补帧，判断带滞回；旁路输出为最近原帧且与对照在相位端点逐字节一致 |
| VQ-28 | 仅增强路径的主线程占用 | 使用 `e2e/enhancement-thread-migration.test.cjs`；受控窗口无长任务；同源同尺寸像素对照不退化；保留原路径回退开关 |
| VQ-29 | 起播与定位后的原画窗口 | 使用 `e2e/interpolation-delay-adaptive.test.cjs`；首帧到补间层就绪与定位后原画窗口比固定 150ms 对照明显缩短；稳定段与定位后音画绝对偏差 ≤ 40ms；正常推进的画面回跳为 0；恢复余量只出现在已经停顿的窗口 |
| VQ-30 | 画质变化可解释性 | 使用 `e2e/quality-observability.test.cjs`；窗口按秒汇总清晰度档位与成本证据，档位变化可从窗口数据与界面说明定位原因 |
| VQ-31 | 高刷与物理呈现 | 使用 `e2e/high-refresh-presentation.test.cjs`；窗口必须分别给出源帧、生成帧、合成器提交与可观察呈现四层计数，并固定标注物理呈现未验证；目标高于刷新能力时应判为「上限」而不是把生成帧率写成呈现帧率；对齐口径下被呈现帧相对刷新边界不超过一个目标帧间隔；边界回调不交付时退回截止时刻计时器且播放不中断。60Hz 在本机记录；120/144/165Hz 需目标设备，按 `SANYE_DISPLAY_HZ` 运行同一入口，设备不具备时保持待环境 |

本轮不把无头浏览器与合成输入的结果当作真实高刷呈现代替证据；缺少显示设备、高速拍摄或呈现工具时保留为待环境。

## 高刷与物理呈现验收（VQ-31，2026-09-21）

更新记录：2026-09-21，v1.18，补充 VQ-31 的分层计量与呈现对齐验收口径；本机窗口、命令与隔离对照数据见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.7。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-31 | 四层计量互相独立 | 每个统计窗口同时给出源帧、生成帧（含补间帧）、合成器提交与可观察呈现；生成帧率达标不能被写成已经呈现；窗口固定带 `physical: 'unverified'` |
| VQ-31 | 目标高于刷新能力时的口径 | 窗口判读为「受刷新上限约束」，结论写「刷新上限」；提交可以多于可观察呈现，且两者的差额必须被记录，不得用其一冒充另一 |
| VQ-31 | 提交落在刷新边界上 | 对齐口径下被呈现帧相对刷新边界的内容陈旧度不超过一个目标帧间隔；隔离对照 `deadline` 保留优化前计时器节拍用于同源比较 |
| VQ-31 | 边界回调不交付的回退 | 刷新边界回调没有实际到达时，退回截止时刻计时器继续推进，且取消未交付的边界回调；播放不中断、不出现高频空转 |
| VQ-31 | 显示时钟不受影响 | 目标不高于刷新能力时仍使用刷新时隙比例，非整数倍刷新（如 120 目标 / 144Hz）不出现追赶突发，帧数保持目标 |
| VQ-31 | 高刷设备复测 | 60Hz 与至少一种 120/144/165Hz 设备分别记录；入口核对实测刷新率与 `SANYE_DISPLAY_HZ` 声明值；设备不具备、缺少高速拍摄或呈现工具时保持待环境 |

## 仅增强路径线程迁移验收（VQ-28，2026-09-20）

更新记录：2026-09-20，v1.15，补充 VQ-28 的验收口径与证据入口；本机窗口、命令与像素对照数据见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.4。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-28 | 仅增强路径的主线程占用 | 640×360、30 FPS 本地 HLS 夹具稳定窗口内页面主线程无长任务（对照：原主线程路径 61 次、最长 65ms）；增强画布持续可见 |
| VQ-28 | 同源同输出尺寸的像素对照 | 同一受控源帧分别经 anime4k.js 单帧管线与渲染 Worker 渲染器；五个档位 RGB 通道逐字节一致（性能/均衡/锐化/修复各 172,800 个通道、超分 691,200 个，最大偏差 0），输出尺寸一致；性能档 alpha 差异单独记录，不计入画质结论 |
| VQ-28 | 档位与补帧语义 | 实际档位等于请求档位；仅增强路径 `interpolationNeed` 为 `none`、合成帧 0、每窗记录跳过的光流分析、停顿帧 0；`forceBypass` 在 24/30/55/60/120 FPS 共 630 个样本上不补帧且无模式变化 |
| VQ-28 | 一键回到原路径与自动回退 | `enhancementPath: 'main'` 时使用原 `anime4k.js` 主线程路径且不创建渲染 Worker；渲染 Worker 模块不可用时自动回退主线程路径，页面无未捕获错误、播放不中断 |
| VQ-28 | 既有播放链路回归 | `progress-seek-latency`（四配置）、`interpolation-bypass`、`enhancement-recovery`、`quality-fluctuation`、`frame-menu-state`、`playback-pipeline`、`startup-quality-timeline`、`playback-experience`、`player-quality`、`realtime-interpolation`、`worker-generations` 与 `enhancement-models` 全部通过 |

未覆盖：页面无法驱动渲染 Worker 持有的 OffscreenCanvas WebGL 上下文，Worker 运行期上下文丢失路径未单独触发（原主线程路径的上下文丢失用例固定 `enhancementPath: 'main'` 保留）；真实显卡、真实动漫片源、跨设备窗口与桌面安装物保持待环境。

## 画质可观测性验收（VQ-30，2026-09-21）

更新记录：2026-09-21，v1.17，补充 VQ-30 的验收口径与证据入口；本机窗口、命令与边界见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.6。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-30 | 窗口汇总 | 一秒窗口同时带上实际清晰度档位（序号、高度、码率）、阶梯最高档、升档需求、窗口尺寸上限、档位来源、带宽估计，以及渲染 Worker 的 `stallFrames`、`analysisWidth`、生效/请求增强档位与 `qualityFallbacks`；清单解析前的未知档位不写入窗口，窗口历史有上限 |
| VQ-30 | 原因归纳 | 非最高档原因按「手动选择 → 记忆档位回落 → 播放窗口尺寸上限 → 带宽不足 → 播放中停顿 → 带宽自适应」取第一个能被同窗口数据证实的原因；处于最高档时不产生原因 |
| VQ-30 | 瞬时提示 | 只在自动档位从已知档位下降到更低档时提示一次，文案为「清晰度已调整：当前 XP（最高 YP）：原因」；同一高度在回到最高档前不重复播报；最小间隔内的降档在下一个窗口补齐；手动选择与起播低档起步不提示 |
| VQ-30 | 界面文案与窗口数据一致 | 真实降档场景下提示文案必须能在窗口历史里找到同一份 `summary`，且「带宽不足」类原因必须满足同窗口带宽估计低于升档需求 |
| VQ-30 | 可查询说明且不占画面 | 清晰度菜单提供「当前清晰度说明」入口，返回最新窗口说明与成本证据；原有「自动 / 各高度档位」选项与控件标签不变，不新增常驻面板 |
| VQ-30 | 既有播放链路回归 | `quality-observability`（4 项）、`quality-fluctuation`、`startup-quality-timeline`（2 项）、`progress-seek-latency`（四配置）、`interpolation-delay-adaptive`（5 项）、`interpolation-bypass`（9 项）、`enhancement-recovery`（6 项）、`realtime-interpolation`（4 项）、`playback-pipeline`（3 项）、`playback-experience`（2 项）、`frame-menu-state` 与清晰度专项 `e2e/e2e-media-quality.mjs`（4/4）通过 |

未覆盖：真实网络下的降档预测准确率、真实片源与显卡的档位分布、跨设备与桌面安装物，以及常驻历史曲线或长期统计入口（本轮按 D-028 采用瞬时提示加菜单查询，不做常驻面板）。

## 补帧延迟自适应收缩验收（VQ-29，2026-09-21）

更新记录：2026-09-21，v1.16，补充 VQ-29 的验收口径与证据入口；本机判定语义、固定延迟对照、命令与边界见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.5。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-29 | 判定语义 | 起播从最小可用窗口（2 个源帧间隔、最低 40ms）起步；未接管呈现前加宽单调不减且封顶 150ms；播放段首个窗口是预热窗口、不据此登记压力；停顿与未命中配对窗口只登记恢复且每次只走一步；连续干净窗口逐级收缩到下限；播放段内恢复与起点恢复分开计数 |
| VQ-29 | 起播原画窗口 | 与固定 150ms 对照在同一进程、同一夹具、同一次运行内对照：首帧到补间层就绪时长明显缩短，且生效延迟 > 0 并 < 150ms |
| VQ-29 | 定位后原画窗口 | 三次拖动进度条（松开 → 补间层恢复）取中位数，明显短于固定 150ms 对照；每次拖动都必须完成定位并恢复补间层 |
| VQ-29 | 音画同步门禁 | 直接读音频延迟节点的实际值：稳定段与定位后的绝对偏差 ≤ 40ms |
| VQ-29 | 不引入画面回跳 | 正常推进时的画面回跳为 0；停顿期间恢复余量造成的回退单独计数，且只出现在已经停顿或未命中配对的窗口 |
| VQ-29 | 不替换补帧本身 | 输出尺寸与生效档位与固定延迟对照一致，30 FPS 源在 60 目标下仍产出合成帧 |
| VQ-29 | 既有播放链路回归 | `interpolation-delay-adaptive`（5 项）、`quality-fluctuation`（1 项）、`interpolation-bypass`（9 项）、`progress-seek-latency`（四配置 1 项）、`realtime-interpolation`（4 项）、`startup-quality-timeline`（2 项）与 `enhancement-recovery`（6 项）全部通过 |

未覆盖：真实显卡与真实动漫片源下的需要量级、长片累计漂移、跨设备与桌面安装物，以及以声学或高速拍摄测量的物理音画同步；这些保持待环境，不用无头软件渲染结果与延迟模型核对替代。

## 起播清晰度记忆验收（VQ-25，2026-09-20）

更新记录：2026-09-20，v1.12，补充 VQ-25 的验收口径与证据入口；本机窗口、命令与边界见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-25 | 有记忆时起播直接使用上次成功的实际档位 | 同一地址第二次起播首帧宽度即为高档，且高档到位不晚于首帧（本机受控夹具四次运行 137.2–244.7ms） |
| VQ-25 | 手动偏好、自动记忆、有效期与清除的存储语义 | 覆盖手动优先于自动、阶梯变化按同高度与最近码率重匹配、匹配不到返回低档、过期与清除后回到无记忆 |
| VQ-25 | 失败与超时回落 | 记忆档位分片延迟 8 秒时，首帧在 2000ms 回落窗口内以低档出画且该地址记忆被作废；页面无未捕获错误 |
| VQ-25 | 冷启动与过期记忆不退化 | 冷启动与记忆过期场景首帧仍为低档，并在约 2.35 秒升到高档，与既有基线一致 |
| VQ-25 | 既有播放链路回归 | `quality-fluctuation`、`progress-seek-latency`、`frame-menu-state`、`frame-cadence-policy`、`interpolation-stability`、`playback-experience` 入口全部通过 |

未覆盖：真实 1080p+ 片源、真实带宽波动与 CDN 换线、跨设备窗口降档上限、桌面安装物；这些保持待环境，不用受控夹具结果替代。

## 增强档位有界恢复验收（VQ-26，2026-09-20）

更新记录：2026-09-20，v1.13，补充 VQ-26 的验收口径与证据入口；本机判定语义、渲染 Worker 窗口、命令与边界见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.2。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-26 | 瞬时抖动不得降档 | 3 帧级尖峰与 0.4 秒抖动结束时必须全部保持 `hold`；四分之一样本超预算、预算内耗时同样不得触发降档 |
| VQ-26 | 持续预算压力必须降档 | 压力段需同时覆盖 800ms 且不少于 3 个样本；增强耗时超预算与队列积压两条路径分别覆盖；增强查询周期性排空只允许推迟一个样本，不得把压力段重新计时 |
| VQ-26 | 恢复需要冷却与稳定门槛 | 冷却 5000ms 内始终 `hold`；冷却到期后仍需连续 3000ms 且不少于 20 个有余量样本才产生一次 `recover`，同一稳定窗口不重复恢复 |
| VQ-26 | 恢复失败退避有界 | 恢复后 20 秒内再次降档记为一次失败，冷却翻倍为 10000ms；时间轴重置（定位、暂停、换源）保留冷却与退避；恢复后再次恢复必须重新满足冷却与稳定窗口 |
| VQ-26 | 不可恢复上限 | 可恢复上限等于实际档位（240 FPS 平衡或结构性降档）时，持续有余量也不得产生 `recover` |
| VQ-26 | 渲染 Worker 端接线 | 受控夹具（640×360、2× 超分、34 秒）出现持续压力降档与至少一次有界恢复；档位每次只升降一档且记录首尾相接；实际档位不高于请求档位与可恢复上限；恢复间隔覆盖冷却与稳定窗口；回退记录包含降档与恢复原因 |
| VQ-26 | 既有播放链路回归 | `quality-fluctuation`、`progress-seek-latency`、`frame-menu-state`、`frame-cadence-policy`、`interpolation-stability`、`playback-experience`、`presentation-alignment`、`player-quality` 入口全部通过 |

未覆盖：真实显卡下可承载请求档位的恢复时机、真实动漫片源、长片与跨设备表现、桌面安装物；这些保持待环境，不用无头软件渲染结果替代。

## 源帧率达标跳过光流补帧验收（VQ-27，2026-09-20）

更新记录：2026-09-20，v1.14，补充 VQ-27 的验收口径与证据入口；本机窗口、对照、命令与边界见[播放器测试报告](./media-player-test-report.md)，实现口径见[播放器开发设计](./media-player-development.md)与[进阶计划](./player-quality-development-plan.md) 6.2.3。

| 编号 | 验收内容 | 通过条件或待环境 |
| --- | --- | --- |
| VQ-27 | 判定语义与滞回 | 60 FPS 源在 60 目标下保持窗口（400ms / 6 样本）结束后进入旁路且只切换一次；51–54 FPS 对 60 目标的滞回带内不进入也不退出；源降到 24 FPS 时在保持窗口（700ms / 8 样本）后恢复补帧；目标由 60 改为 120 时按新目标恢复，改回 60 后重新旁路；偶发丢帧样本与时间轴重置不改变当前模式 |
| VQ-27 | 旁路执行范围 | 命中旁路的窗口内缩放、光流分析与运动场往返计数为 0，`computeMs` 为 0，`interpolatedFrames` 为 0，`flowSkippedFrames` 大于 0；帧仍持续上传并呈现 |
| VQ-27 | 真实播放链路 | 640×360、60 FPS 本地 HLS 以「补帧目标 60 + 画质 fast」播放时，判定完成后源帧率估计为 60，窗口内分析次数 0、合成帧 0、停顿帧 0，增强画布保持可见且无页面错误 |
| VQ-27 | 同源成本对照 | 同一 320×180 夹具以 `interpolationDemand: 'always'` 强制保留补帧时，运动场纹理上传使每帧纹理上传中位数达到旁路的 2 倍以上（实测 3.05 对 1.0） |
| VQ-27 | 像素输出 | 旁路在帧窗口中点输出最近原帧（与源帧逐字节相同），对照在该点输出相邻帧之间的合成色；相位 0 与 1 上两路输出逐字节一致 |
| VQ-27 | 增强与档位不受影响 | 请求超分时旁路仍执行增强处理（`enhancementMs > 0`）、输出尺寸跟随实际增强档位、呈现时隙不为 0；VQ-26 的降档与有界恢复语义不变 |
| VQ-27 | 既有播放链路回归 | `quality-fluctuation`、`enhancement-recovery`、`startup-quality-timeline` 入口全部通过；`player-quality` 四项中三项通过（1080p 高刷矩阵的末窗帧率门禁在本机负载下未通过，保留为未通过项）；`pnpm typecheck`、`pnpm build` 通过 |

未覆盖：真实动漫片源、跨显卡与跨设备判断、桌面安装物、物理刷新呈现；本机 1080p 高刷门禁（`realtime-interpolation` 的 live 项与 `player-quality` 的高刷矩阵）在当前负载下未通过，其中前者已做改动前后对照且两版都失败（见[播放器测试报告](./media-player-test-report.md)），不由本次改动的受控窗口结果替代。

## 画质进阶 P3 验收（2026-09-18）

更新记录：2026-09-18，v1.7，补充第四步 VQ-10～VQ-12 的隔离候选验收；本机结果及未测量项见[播放器测试报告](./media-player-test-report.md)。

- 固定代码提交、ncnn 版本、原生 EXE 和 v4.6 权重 SHA-256，分别核对代码与模型许可；记录文件体积、GPU/驱动和原生特性输出。标签和摘要不等于可复现构建证明。
- 六类自生成解析场景使用 `.2/.4/.5/.6/.8` 时间步；每格保留首次启动和三个统计样本。同源光流输出使用相同 PNG、尺寸、颜色方向、真值和指标实现。记录 MAE、PSNR 与注明变体的全局 SSIM，逐场景保存退化，不用合成纹理推定真实动漫观感。
- 连续模式用 5 张 1080p 输入生成 21 张输出，检查退出码、实际文件数、解码后尺寸及摘要；每次均使用新目录，防止旧结果掩盖无输出。拒绝目录复用，并覆盖缺模型、非零退出、输出缺失与超时等失败记录。
- P50/P95 明确样本数和计时边界；进程工作集与显存分开。每次 CLI 启动包含模型加载，预运行只预热系统缓存，不能标作常驻模型预热。文件读写、浏览器读回和生产帧传输不等价。
- 质量、实时端到端预算、依赖分发任一条件未满足，允许形成暂不采用决策；常驻推理、实际传输、显存、真实素材及目标设备缺口保留待验证，不为进入 P4 降低门禁。

## 画质进阶 P2 验收（2026-09-18）

更新记录：2026-09-18，v1.6，补充 VQ-07～VQ-09 验证；实测、失败候选与未达边界见[播放器测试报告](./media-player-test-report.md)。

- 固定 P1 视频模块快照 `pre-p2/`。运动验证同时检查平移、旋转、形变、遮挡、细线和切镜的向量误差、覆盖率及实际中间帧；不能用减少有效向量数量直接推导视觉改善。原生稠密流记录库版本、线程数和参数，不将原生耗时当作浏览器性能。
- 资源验证比较 256 宽及 1080p 的原画、修复与超分输出，检查连续变化内容、颜色、方向与像素摘要；覆盖尺寸切换、池上限、重复释放、处理中断、查询结束和同上下文恢复。资源估算注明范围，不记为整机可用显存。
- 比较原帧增强复用与逐个输出帧增强的成本和像素差异，保留初始化成本，禁止只比较省略了预处理的一侧。
- 原画、性能、均衡、锐化、修复、超分分别测试 60/120 目标，锐化另测 90/144/165/240 和自动，共十七种组合。记录请求、实际档位、实际目标、回退历史与资源数据。最高档不足时检查菜单实际选中项和保留的用户偏好，不能标作最高质量已经生效。
- 复验现有运动、像素、生命周期及短时高刷门禁，执行类型检查、构建与文档检查。GPU 专项串行运行；同源基线使用同一设备和输出尺寸，保留不达标窗口。

## 画质进阶 P1 验收（2026-09-18）

更新记录：2026-09-18，v1.5，补充第二步 VQ-04～VQ-06 的对照与生命周期验证；实际命令、失败候选和最终结论见[播放器测试报告](./media-player-test-report.md)。

- 原实现与候选使用相同 Sintel 原片摘要、20～35 秒范围、1080p 输出、锐化档、120 目标和四类阶段参数。保存修改前源码，运行首尾计算整个视频模块及测试入口摘要，避免遗漏新增调度模块。
- 调度覆盖孤立长任务、连续计时器节流、稳定退出与再次进入、有界消息任务、60 档、停止、暂停和隐藏。真实播放同时记录完成帧数、实际帧间隔、迟到、调度调用数和 CPU；禁止通过降低输出尺寸或回放过期媒体时刻满足门禁。
- 策略覆盖冷启动、切源、循环、定位、暂停恢复、倍速和分辨率变化；旧会话返回不可清除新会话忙状态或混入采样窗口。精度升降必须有滞回，不保留失效的升档计数。
- 设备能力持久化按设计评估决定暂缓；存储禁用时仍可开启、停止和重建补帧。当前显示器的 60Hz 限制及包内、物理高刷、功耗验证边界保持明确。

## 画质进阶 P0 基线验收（2026-09-18）

更新记录：2026-09-18，v1.4，补充[画质进阶计划](./player-quality-development-plan.md)中 VQ-01～VQ-03 的测量验收。历史媒体功能测试保持原日期；本轮执行结果以[播放器测试报告](./media-player-test-report.md)为准。

- 样本清单覆盖平移、快速动作、非刚性形变、遮挡、细线及字幕、渐变、切镜和低码率噪声，逐项保存摘要、尺寸、帧率、片段时间与来源授权。生成规则与种子固定，素材留在本地证据目录。真实片源与合成素材、真值与主观评估分别标记；未提供授权真实片段时保留待环境。
- 测量区分主线程和 Worker 的单调时钟、跨线程排队、CPU 提交和 GPU 完成，记录单位、抽样数与不可测原因。解码信息只使用浏览器实际提供的元数据，不能把捕获等待时间命名为解码成本。
- 冷启动、预热、持续播放和负载干扰分别保存原始窗口与汇总；保留启动异常、回退、未达帧率窗口和中断记录。CPU 覆盖浏览器相关进程，注明逻辑核归一化，内存注明观测范围；不可读取的显存与能耗不写零。
- 对照必须核对输入摘要、输出尺寸、目标帧率、增强档、设备驱动、浏览器版本、供电与显示模式以及阶段参数。代码身份包含未提交与未跟踪的实际源码摘要；单个 Git 提交不能代表脏工作区，缺失的构建或环境证据不能推定一致。
- 增加观测后复验原有补帧生命周期和针对改动的策略测试，执行类型检查、构建与文档校验。GPU 基线与其他 GPU 测试串行执行，构建不与性能采样同时运行。样本截图用于确认内容有效，不能替代真实动漫主观观感或物理屏幕高刷验收。

## 当前核对（2026-09-10）

当前自包含浏览器回归包含电影线路 2 项和清晰度 4 项；本次未访问第三方真实播放源。跨集线路、外部来源防火墙和授权播放需要不同测试环境，不能由受控 HLS 夹具推定全部可播；证据见[当前审计](./current-status-audit.md)。

更新记录：2026-09-10，v1.3，按当前代码与进度校正本文事实或证据范围；依据上述源码、任务与审计引用。

## 1. 单元测试

后端至少覆盖：

- 仅接受 `https` 和配置白名单域名。
- 拒绝空地址、非法 URI、非目标来源域名、超过最大选集数的结果。
- 解析页面标题、同源选集链接和可见 `iframe` 地址。
- 页面没有 iframe、页面超时、响应过大时返回稳定业务错误。
- 同一作品重复导入时按剧集编号替换，不产生重复数据。
- PostgreSQL 存储的查询、批量替换和字段映射。

前端至少覆盖：

- API 类型和路径与接口契约一致。
- 详情页加载剧集失败不影响作品元数据。
- 选集切换只改变当前 ArtPlayer 播放地址，并销毁上一集的 HLS 实例。
- 剧场版显示“正片播放”和语言/线路选项，即使接口返回多个媒体记录也不得显示“第 N 集”。
- 多码率 HLS 显示“自动”和真实分辨率档位；清晰度切换只切换 `currentLevel`，不重建 ArtPlayer；单清晰度来源隐藏清晰度菜单。
- iframe 具有 `sandbox` 和 `referrerpolicy` 属性；ArtPlayer 容器生成的 `<video>` 具有 `controls`、`playsinline` 和 `preload="metadata"`。

## 2. 接口测试

| 编号 | 检查 |
| --- | --- |
| M-API-01 | 未发布作品读取剧集返回 `2003` |
| M-API-02 | 已发布作品无剧集返回 `200` 空数组 |
| M-API-03 | 未携带管理调用方凭证导入返回 `2002` |
| M-API-04 | 非白名单来源导入返回 `1001` |
| M-API-05 | 合法来源导入后可读取剧集数量与来源页 |
| M-API-06 | 重复导入结果幂等 |

## 3. Playwright 验收

- 详情页存在播放区时，HTML 来源显示隔离 iframe，视频来源显示 ArtPlayer 控制栏和剧集选择器。
- 真实来源 HLS 页面渲染 ArtPlayer 容器及其 `<video>`，首集清单和分片链路可加载，无 `player-overlay` 错误态。
- 点击另一集后播放器 `src` 更新，当前剧集标题更新。
- 无剧集时显示空状态，不出现空 iframe。
- 外部播放器加载失败时显示失败提示和重新加载按钮。
- 模拟多码率 HLS 显示 `自动、720P、360P`，选择 `360P` 后请求对应子清单且控制栏保持存在。
- 详情页在 390px 宽度下无横向溢出，播放器保持比例。
- 管理端只有有 `anime:content:edit` 权限的用户看到导入按钮。
- 管理端输入非法地址时不发起导入请求，合法地址能显示导入数量。

## 4. 本地命令

```powershell
cd sanye_server
mvn -pl sanye-server-anime -am test
cd ..
pnpm --filter @sanye/sanye_client typecheck
pnpm --filter @sanye/sanye_client build
pnpm e2e:verify
pnpm e2e:media-quality
pnpm docs:check
```

## 5. 通过标准

- 新增后端单元测试全部通过，既有 Maven 测试不得回归。
- 客户端和管理端类型检查、构建通过。
- Playwright 播放器和管理端导入场景通过；无 JS、资源、CORS 错误。
- `pnpm docs:check` 通过，接口、迁移、配置和任务文档同步。
- 外部来源真实播放只记录为“环境验证”，不把第三方服务暂时可用写成项目自身媒体服务已就绪。

## 6. 本次执行结果

详细报告见 [媒体播放器测试报告](./media-player-test-report.md)。本次本地联调结果如下：

| 板块 | 结果 | 证据 |
| --- | --- | --- |
| anime 媒体单元测试 | 65/65 通过，失败 0 | Maven + JaCoCo 门禁通过 |
| 后端全量单元测试 | 210/210 通过，失败 0 | `mvn -f sanye_server/pom.xml test` |
| 媒体播放器 Playwright | 4/4 通过 | `e2e/e2e-media-player.mjs` |
| 真实来源 HLS 与季度/搜索聚合 Playwright | 34/34 通过 | `e2e/e2e-media-player-live.mjs` |
| 多码率清晰度专项 Playwright | 3/3 通过 | `pnpm e2e:media-quality` |
| 电影语言线路专项 Playwright | 2/2 通过 | `node e2e/e2e-movie-playback-lines.mjs` |
| 全量 Playwright | 11 个脚本合计 548/548 通过；其中按钮体检 428/428 | `pnpm e2e:all` |
| 前端与桌宠类型检查 | 3/3 工程通过 | `pnpm typecheck` |
| 前端生产构建 | client/admin/pet 均通过 | `pnpm build` |
| 接口分板块体检 | 60/60 通过，失败 0 | `interface-check.ps1` |
| 文档一致性检查 | 572/572 通过，失败 0 | `pnpm docs:check` |

管理端导入的真实来源页面和外部播放器仍未作为正式生产验收依据，版权授权、来源条款和持续可用性继续由 `GAP-016` 管理。

## 更新记录

| 日期 | 版本 | 变更 | 依据 |
| --- | --- | --- | --- |
| 2026-08-21 | v0.1 | 建立单元、接口、Playwright 和通过标准 | 媒体开发设计 |
| 2026-08-21 | v0.2 | 登记已执行的 anime 单测 58 项、播放器 Playwright 4/4 和前端类型检查证据 | T-D-08 验证结果 |
| 2026-08-21 | v0.3 | 补充全量 Maven、全量 Playwright、接口体检、构建和文档检查结果 | 最终本地联调验证 |
| 2026-08-24 | v0.4 | 增加真实 HLS 浏览器链路、五个无职转生篇章聚合和当前 65/210 单测、22/22 专项、563/563 全量 E2E 证据 | `e2e-media-player-live.mjs`、`mvn test`、`pnpm e2e:all` |
| 2026-08-24 | v0.5 | 运行目录、内存回退和管理端回退均收口到六部正式作品；全量 E2E 当前为 539/539，真实 HLS 专项仍为 28/28 | `AnimeMemoryStore.java`、`contentView.vue`、`pnpm e2e:all` |
| 2026-08-24 | v0.5 | 补齐《你的名字》两条可播放线路；专项增加真实播放推进检查，覆盖 6 个作品、季度聚合和搜索聚合共 28/28 | `e2e-media-player-live.mjs`、`GET /api/v1/anime/127/episodes` |
| 2026-08-24 | v0.6 | 增加 ArtPlayer 外壳、控制属性和 HLS customType 断言，保持真实 HLS 专项 28/28 通过 | `e2e/e2e-media-player-live.mjs`、`pnpm --filter @sanye/sanye_client typecheck` |
| 2026-08-24 | v0.7 | 将季度验证改为独立卡片、独立封面和独立详情入口，移除系列聚合断言 | `e2e/e2e-media-player-live.mjs`、`animeRepositoryView.vue` |
| 2026-08-24 | v0.9 | 同步完整回归后的全量 Playwright 当前基线 548/548 和按钮体检 428/428 | `pnpm e2e:all`、`node e2e/e2e-buttons.mjs` |
| 2026-08-24 | v1.0 | 清理 E2E 临时数据后同步当前基线：全量 Playwright 540/540、独立按钮体检 425/425（完整套件内 420/420） | `pnpm e2e:all`、`node e2e/e2e-buttons.mjs` |
| 2026-08-24 | v1.1 | 增加多码率 HLS 清晰度专项 3/3，并将真实 HLS/季度/搜索聚合更新为 34/34、全量 Playwright 更新为 548/548 | `e2e/e2e-media-quality.mjs`、`e2e/e2e-media-player-live.mjs`、`mvn test` |
| 2026-08-25 | v1.2 | 增加剧场版正片和语言线路语义检查，确认两条媒体记录不会显示为第一集、第二集 | `e2e/e2e-movie-playback-lines.mjs`、客户端 typecheck/build |
