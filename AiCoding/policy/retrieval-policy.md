# 检索策略

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1 |
| 文档状态 | 基线 |
| 更新时间 | 2026-08-26 |

- 先按 `index/retrieval-guide.md` 读取入口，再读取任务局部。
- 使用 `rg` 定位符号、接口和章节；不要为了搜索而加载整个大文件。
- 优先读取当前代码和唯一事实源，历史记录只用于解释变更原因。
- 不把 `node_modules`、构建产物、日志和临时目录纳入 AiCoding 事实。
- 发现断链、过期状态或来源冲突时立即登记，不在摘要中静默修正。
