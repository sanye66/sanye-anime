# 文档冲突处理

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v0.1 |
| 文档状态 | 基线 |
| 更新时间 | 2026-08-26 |

冲突裁决顺序：

1. 当前可运行源码和测试证据。
2. `product/` 或 `docs/` 中标记为基线的唯一事实源。
3. 最近日期且有命令或报告证据的记录。
4. 历史 Ledger 和外部讨论，只作为变更背景。

无法裁决时保留两个来源、写明差异和解除条件，状态标记为 `评审中` 或 `待环境`，不得凭猜测覆盖。
